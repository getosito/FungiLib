import Layout from "../components/Layout";

export default function Login() {
    return (
        <Layout>
            <h1 style={{ marginTop: 0 }}>Login</h1>
            <p>Login page (to be connected with Firebase Auth).</p>
        </Layout>
    );
}
